

mutation OtpToToken($login: String!, $password: String!) {
    otpToToken(login: $login, password: $password) {
        token {
            expiration
            expiration_date
            token
        }
    }
}